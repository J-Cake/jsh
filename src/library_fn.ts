import {Runner, Running} from "./statement.js";
import {LineTree} from "./command.js";

export default async function library_fn(args: LineTree): Promise<Running> {
    throw new Error('Not implemented');
}
